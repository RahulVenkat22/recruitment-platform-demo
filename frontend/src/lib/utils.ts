/**
 * Class-name merge used by every component: clsx-style arguments with
 * Tailwind conflict resolution (the last utility in a group wins).
 * Backed by the compiled `cn` package that shadcn 4 ships with.
 */
export { cn } from 'cn'
